server.port=8080
spring.application.name=GreenLoop

spring.datasource.url=jdbc:postgresql://localhost:5432/greenLoop
spring.datasource.username=postgres
spring.datasource.password=Linhvovip
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.main.allow-circular-references=true

# Email Configuration (defaults for development)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=greenloopcyc@gmail.com
spring.mail.password=mubkcunbpryolcpj
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Cloudinary Configuration (defaults for development)
cloudinary.cloud_name=dmpjc496u
cloudinary.api_key=867162548936863
cloudinary.api_secret=t_Wp6_Yoc8xLv0nXXfqO-gIVF8I

# OpenAPI Configuration
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true

# JWT Configuration (default secret for development)
app.jwtSecret=${JWT_SECRET:mySecretKey1234567890abcdefghijklmnopqrstuvwxyz}
app.jwtExpirationInMs=86400000

# Frontend URL Configuration
app.frontend.url=${FRONTEND_URL:http://localhost:3000}

# Logging Configuration
logging.level.root=INFO
logging.level.org.greenloop.circularfashion=DEBUG
logging.level.org.springframework.security=DEBUG

# Redis Configuration (optional for development)
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.username=${REDIS_USERNAME:}
spring.data.redis.password=${REDIS_PASSWORD:}

# Spring Boot Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics,prometheus,env,configprops
management.endpoint.health.show-details=always
management.endpoint.health.show-components=always
management.prometheus.metrics.export.enabled=true
management.metrics.tags.application=greenloop-backend
management.metrics.distribution.percentiles-histogram.http.server.requests=true
management.metrics.distribution.percentiles.http.server.requests=0.5,0.95,0.99
management.metrics.enable.jvm=true
management.metrics.enable.process=true
management.metrics.enable.system=true
management.metrics.enable.logback=true
management.metrics.enable.tomcat=true
management.metrics.enable.hikaricp=true 